#ifndef SORT_H
#define SORT_H

// declearation for sorting implementation
void selectionSort(int arr[], int length);
// decleration for swapping passed values
// void swap(int &value1, int &value2);
// declearation for print
void printArray(int data[], int size);
// declearation for random generator
int getRandom(int min, int max);
// decleration to fill array with random numbers
void fillArray(int data[], int size, int min, int max);

#endif
